# Used by the SvelteKit node process when fetching /admin/maze.
MASE_SERVER_INTERNAL_HTTP_URL=http://localhost:8080

# Returned to the browser for reset requests and live WebSocket events.
PUBLIC_MASE_SERVER_HTTP_URL=http://localhost:8080
# PUBLIC_MASE_SERVER_WS_URL=ws://localhost:8080/ws
